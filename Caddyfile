:80 {
    reverse_proxy app:8080
    
    # Rate limiting: 100 requests per minute per IP
    rate_limit {
        zone dynamic {
            key {remote_host}
            events 100
            window 1m
        }
    }
    
    # Enable basic metrics if needed, or structured logs
    log {
        output stdout
    }
}
